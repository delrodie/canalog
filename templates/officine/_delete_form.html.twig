<form method="post" action="{{ path('officine_delete', {'id': officine.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cette officine?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ officine.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
