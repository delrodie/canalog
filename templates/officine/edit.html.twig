{% extends 'base.html.twig' %}

{% block title %}Edit Officine{% endblock %}

{% block body %}
    <div class="jumbotron jumbotron-fluid text-left">
        <div class="container">
            <h1>Gestion des officines</h1>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <div class="card shadow-sm p-3 mb-5 bg-white rounded">
                    <div class="card-body">
                        <div class="card-title mb-5"><h4 class="h4">Liste des officines</h4></div>
                        <table class="table table-striped">
                            <thead>
                            <tr>
                                <th>Id</th>
                                <th>Nom</th>
                                <th>actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for officine in officines %}
                                <tr>
                                    <td>{{ loop.index }}</td>
                                    <td>{{ officine.nom }}</td>
                                    <td>
                                        <a href="{{ path('officine_show', {'slug': officine.slug}) }}"><i class="fa fa-eye"></i></a> |
                                        <a href="{{ path('officine_edit', {'slug': officine.slug}) }}"><i class="fa fa-edit"></i></a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="6">Aucune office enregistrée</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card shadow-sm p-3 mb-5 bg-white rounded">
                    <div class="card-body">
                        <div class="card-title mb-5">
                            <h4 class="h4">Modification</h4>
                        </div>
                        {{ include('officine/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                    {{ include('officine/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}


